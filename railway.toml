[build]
builder = "RAILPACK"
buildCommand = "pnpm install --frozen-lockfile && rm -rf .next && pnpm build && node scripts/copy-standalone-assets.mjs"

[deploy]
startCommand = "node scripts/start-standalone.mjs"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
