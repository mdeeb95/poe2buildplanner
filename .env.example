# Optional — production needs no secrets today.

# Dev server port (scripts/dev.sh)
# PORT=3000

# Railway build cache directory (scripts/railway-build-lib.ts)
# BUILD_CACHE_DIR=/root/.cache/buildproj-next
